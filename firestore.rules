rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Anyone can read (public site); only authenticated admin can write.
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
